@model IEnumerable<CurApi.Models.ProFaCurdef>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurCd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurDispNm)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CurSecCur)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CurCd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurDispNm)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CurSecCur)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CurPK }) |
            @Html.ActionLink("Details", "Details", new { id=item.CurPK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CurPK })
        </td>
    </tr>
}

</table>
